@ВПроекте
@НаСервере @ДоступноСКлиента
статический метод ЭтоАдминистраторПриложения(): Булево
    знч ТекущийПользователь = Пользователи.ТекущийПользователь
    если ТекущийПользователь == Неопределено
        возврат Ложь
    ;
    знч Запрос = Запрос{
        ВЫБРАТЬ Администратор Из Пользователи Где Ссылка == %ТекущийПользователь;
    }
    
    возврат Запрос.Выполнить().Единственный().Администратор
;
